# 457 Property Visitor Map — Backlog

Organized by intended timing rather than numeric priority. Within each section, items are roughly ordered by likely implementation sequence. Move an item to **Completed** only after the implementation has been verified live.

## Now

- **Confirm the September 18 mobile-control and audit cleanup live.** Check the tightened right-side control spacing; compact Info icon and stable Experimental grouping after repeated resets; always-on Dirt roads; trail taps on Main Loop, Mountain Drive, and Powerline Trail; aerial/topo unfinished-trail contrast; legend dismiss, restore, and reset behavior; and the Home popup with no photo or layout jump.
- **Confirm the September 18 GPS and guest-map update live.** Check the reinforced four-traversal Beeline and its endpoint joins; the new Back Cut Through alignment and Main Loop Ext junction; the compact phone and expanded desktop legends; concise popup wording; and About this map inside the Layers panel. Yard Area remains withdrawn under `_local/withdrawn/`.
- **Field-test the August 28 full-perimeter guidance tint.** The prior full-turn test confirmed compass input, tint at every angle, correct turn-direction text, and the destination ring. Outdoors in daylight, confirm the brighter tint travels continuously around the full screen perimeter, remains clearly visible at 180°, rewards the ≤8° locked state without dimming, and leaves the top-center pill readable. Confirm Reduce Motion removes breathing without weakening the tint; do not close this from desk geometry checks.
- **Finish confirming the August 28 focus, popup, marker, and framing repairs live.** The owner confirmed on desktop that pointer focus boxes are gone and Property Map frames the full property; recheck its slightly tighter framing on desktop and phone. From a far saved view, each home-page route link must make one visible move directly to the complete route on the first attempt. Quickly replace a slow-photo point popup with another point and confirm the second content is never overwritten; photo-less points must open without a teardown resize. The Barbershop and building pins must draw above intersections and corners, while every mapped pin remains above the blue location dot and its accuracy circle remains below property and route layers. Retain the responsive ordinary-popup width and 4:3 point photos. Keep here until phone and desktop confirmation.
- **Confirm the August 28 local trail-cam capture test.** Using deliberately fake sites in `?view=owner`, enable Trail cam sites and desk-test selecting a site and camera, setting/replacing a map-tap bearing arrow, confirming an append-only record, cancelling at every stage, newest-first site history, Reset to default preserving records, and JSON export with clipboard allowed and blocked. Confirm visitor mode has no cam layer, pane, controls, or data request. This version is local-only and must never receive real site coordinates; keep here until the owner completes the table test.
- **Confirm the August 28 map-interaction repair live.** With every overlay enabled, roads, trails, routes, both parcel boundary lines, the powerline corridor, Front Field zone, and every unobscured intersection should respond on phone and desktop; the Pavilion pin intentionally remains above its coincident intersection. Parcel interiors must remain inert, rapid double-tap zoom must leave later popups working, both home-page route cards must render, and the two parcel popups must use the approved short wording. Keep here until confirmed on the published site.
- **Confirm the August 25 route-interaction update live.** Mountain Drive and Driveway should remain separate labeled road files and paths while rendering together in Dirt roads; Mountain Drive alone remains a moderate route. Confirm both roads open popups on mouse and touch, hovering either segment of a multi-segment route emphasizes the complete route, click selection remains stronger, rapid double-tap zoom never leaves feature popups suppressed, and the wider desktop route-details panel opens low on the map without hiding controls. Closing that panel with its X should restore the route popup. Keep here until phone and desktop confirmation.
- **Confirm the August 11 Batch C schema, validator, and derived route elevations live.** Road popups with omitted optional text must never show `undefined`; both existing route cards, map summaries, gain/loss totals, and elevation charts must look identical even though their generated values now derive from segment data at load. Confirm the home and map route panels on phone and desktop. The validator should pass with only the three documented warnings for unrouted trails that lack elevation profiles.
- **Confirm the August 11 Batch B layer-registry and manifest refactor live.** Visitor first load and `?view=owner` must show the same layer set, ordering, styles, and defaults; Corner markers and Intersections remain off by default in both views. Confirm an existing saved v1 base map, view, and layer selection migrates intact, each audience remembers its own settings, Reset to default is unchanged, deep links still focus without replacing saved preferences, and a partial data failure leaves the rest of the map usable. With location active near a mapped pin, landmark, corner, and intersection symbols must remain above the blue location dot; its accuracy circle remains below property and route layers. Keep here until phone and desktop confirmation.
- **Confirm the August 11 Batch A repository hardening live.** The renamed landmark should appear as The Barbershop at its unchanged coordinate; deep-linked routes and features must not overwrite the previously saved map view; parcel interaction must continue working after display-name edits; route cards and popups must switch units at 0.10 mile while live landmark and guidance text stays at 0.15 mile; map popup actions must remain reliable through the consolidated delegated handler; and the cache-busted shared route-detail styling and color tokens must produce no visual restyling. Keep here until phone and desktop confirmation.
- **Confirm the August 9 route and map-interaction refinements live.** Driveway and Mountain Drive should open details through their wider invisible interaction lines on both touch and mouse input. Mountain Drive should also appear as a provisional moderate, 0.26-mile round-trip route with its GPS-derived elevation profile. On mobile, a selected road or every segment in a selected route should remain emphasized while its popup is open. A route popup should offer `Route details` beside its difficulty and distance and open the same elevation panel used on the home page, without the home-only map link. The home-page `View route on map` link should fit the entire route without popup auto-panning it out of view. Route cards and popups should switch from feet to miles at 0.10 mile; live landmark and guidance distances should continue switching at 0.15 mile.
- **Confirm the August 8 mobile-map and home-route refinements live.** On mobile, the Layers list should have larger option rows, trail taps should open details without a tooltip flash, Reset to default should end active guidance, and the guidance destination ring should be compact and centered on the landmark pin. Driveway and Mountain Drive should open details on both touch and mouse input. Confirm that the obsolete Mountain Drive southwest marker is gone while the southern Mountain Drive/Driveway intersection remains. On the home page, verify the QR/map/routes left-to-right layout where space permits, the `0.38 mi round trip` label, and the route-card elevation popout with full-route gain/loss.
- **Confirm the August 7 annotated map refinements live.** Driveway and Mountain Drive names should appear on desktop hover; route and landmark hit targets should be easier to acquire without interfering with nearby features; clickable map features should receive a subtle hover emphasis; active landmark guidance should visibly pulse around its destination; and route popups should show the standard green-circle/blue-square/diamond difficulty mark plus length in feet below 0.15 mile or miles otherwise. Confirm that Main Loop Ext now appears as one consolidated out-and-back centerline and that Mountain Drive joins the unchanged driveway alignment cleanly.
- **Confirm the August 5 GPX intake pipeline and map additions live.** The manifest-driven tool, safety tests, candidate QA report, new trails and driveway, owner-confirmed shared clearing, clean Main Loop/driveway connection, field-zone clearance, The Barbershop landmark, easy Main Loop Route, home route list, and reusable `?feature=` map focus are implemented in the local publishing package. Pre-promotion target backups remain in the ignored candidate area. Keep here until the geometry, route link, landmark, and phone/desktop presentation are confirmed on the published site.
- **Confirm the processed GPS feature layers live.** Mountain Drive remains a provisional dirt road; the two walking trails use unhaloed light-blue dashes; Home and Pavilion use green building pins; Front Field is a subtle dotted/shaded zone; and the owner confirmed that the optional intersection markers display correctly when their layer is selected. Confirm the Pavilion symbol separation, Mountain Drive/Pavilion Side Trail join, newly inset Garden/field junction, and all endpoint positions on phone and desktop.
- **Confirm the corrected powerline corridor live.** The southern connector now runs directly from `SL_SCorner` to `MP_WCorner`, with the remaining edges following parcel vertices. The corridor has a light-gray treatment and explains on desktop hover or mobile tap: “National Grid powerline cut — not our land, but access is allowed.” Confirm the shape and that mobile shows only one explanation popup.
- **Confirm the revised outside-area hatching live.** Owned parcels and the powerline corridor remain clear while a limited surrounding area is lightly brightened with darker orange diagonal dashes. Confirm that the outside area is distinct without looking off-limits on both aerial and topo views.
- **Confirm the cleaned-up landing page and map controls live.** Desktop home content is centered and the unnecessary footer warning is removed. On the map, Home, Info, Layers, and Locate use a compact icon layout; the Layers icon remains visible while its panel opens to the left; and the disclaimer appears only through Info and closes by X, outside tap, or Escape. Keep here until phone and desktop are confirmed on the published site.
- **Confirm map interaction refinements live.** The Main Parcel description now uses a true line-only tap target and should never open from the parcel interior. Rapid double-taps on a trail, zone, or other feature should zoom without also opening its popup. The owner confirmed the zoom level, double-tap zoom, and saved layer/view settings; a Layers-panel button now resets the map to the simple default view.

## Soon

- **Refine parcel labels and acreage display.** Reduce how much imagery the permanent labels obscure. Likely make visibility respond to zoom while keeping parcel details available by tapping/clicking.
- **Expand responsive navigation when more destinations exist.** The current map has a floating Home link. Consider a minimal mobile bottom bar once Info or other destinations are added; adapt navigation appropriately for desktop rather than maintaining a separate site.
- **Continue refining verified outbuildings and key landmarks.** Add or improve points in the same layer as better field observations become available, retaining name, type, notes, and source context.
- **Expand the trail and intersection network.** The repeatable GPX-to-GeoJSON process and hidden intersection dataset now exist. Add trails and confirmed intersections only when their real locations are sufficiently reliable.

## Later

- **Expand point-popup photos only when needed.** The current one-photo-per-point filename convention intentionally has no captions or variants. Seasonal alternatives, multiple photos per point, and captioning remain future options rather than additions to the first simple mechanism.
- **Device-specific location recovery help.** When location fails, provide brief instructions suited to the visitor's device/browser without cluttering the normal experience.
- **Refine the powerline walking-path context.** The first provisional Powerline Trail recording is mapped; improve its alignment when additional tracks arrive. Do not expand the separate corridor polygon without enough evidence.
- **Replace Mountain Drive with stronger GPS coverage.** Current line combines one uphill and one downhill pass and is intentionally marked provisional/work in progress.
- **Visitor-added observation points.** Let a visitor drop a point and add a note for an animal sighting, fallen tree, or similar observation. Local-only storage is easy but remains on that device; sending it to the owner requires a deliberate sharing or backend approach. A “drop point + note + Share” workflow may be the simplest privacy-conscious version.
- **Permanent QR/signage.** Generate a static QR that directly encodes the final GitHub Pages URL—no redirect or expiring QR service. Print the URL beneath it as a fallback.
- **Optional per-sign QR visit log in Google Sheets.** Give each sign a distinct URL/ID and use a small Google Apps Script endpoint to append visits to a private Sheet. Easy default fields: timestamp, sign ID, destination page, browser/device category, language, screen size, timezone, and referrer when available. Precise GPS would remain optional and permission-based. Before implementation, decide what notice visitors see, how long records are kept, and how to reduce bot/link-preview false positives; the public page must not contain a secret that grants Sheet access.

## Completed

### 2026-08-02

- **Approximate parcel boundaries.** Main Parcel and Sliver load from separate validated GeoJSON, with clear owned-area styling and the required not-a-survey notice.
- **Mobile live location.** User-initiated Find me flow, continuous high-accuracy updates, visible location dot, and GPS accuracy circle. Confirmed working in Safari by the owner.
- **Aerial and topo basemaps.** NYS 2022 orthoimagery is the default; OpenTopoMap is the optional topo view. Both confirmed live.
- **Optional corner markers.** Labeled corner-point layer confirmed live.

### 2026-08-03

- **Protected, user-controlled GitHub workflow.** The owner created a stable pre-access release, protected the live branch, and created a separate private backup repository. Direct work-account connector access is not assumed; local changes are reviewed and published by the owner, with stable releases and isolated backup ZIPs used for recovery.
